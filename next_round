#include <iostream>

using namespace std;
int main() {
  int n, k;
  cin>> n >>k;
  int ar[n];
  int count = 0;
  for (int i = 0; i<n; i++){
    cin >> ar[i];
  }
  int comp = ar[k-1];
  for (int i = 0; i<n; i++){
  if (ar[i] >= comp && ar[i] >0)
    count++;
  }
  cout<<count;

}